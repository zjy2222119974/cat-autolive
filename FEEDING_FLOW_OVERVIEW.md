# 冻干喂食器自动化 - 流程概览

## 🎯 核心流程（两步走）

```
┌─────────────────────────────────────────────────────────┐
│  主函数: feed(hwnd, capture_func, portions=1)          │
└─────────────────────────────────────────────────────────┘
                        ↓
        ┌───────────────┴───────────────┐
        ↓                               ↓
┌───────────────────┐         ┌──────────────────────┐
│  步骤1: 导航       │         │  步骤2: 选择并确认    │
│  找到"喂食份数"    │  ───→   │  点击份数 + 确认      │
└───────────────────┘         └──────────────────────┘
```

---

## 📍 步骤1: 导航到喂食份数弹窗

### 三级优先级策略

```
每次尝试 (最多3次):
    ↓
┌─────────────────────────────────────┐
│ 1. 截图 + OCR识别所有文字            │
│    输出: ['涂鸦', '喂食计划',        │
│           '喂食份数', '喂食记录']    │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│ 【优先级1】查找"喂食份数"按钮        │
│                                     │
│  找到? ─── YES ──→ 点击 ──→ ✅ 成功  │
│    │                                │
│   NO                                │
└────┼────────────────────────────────┘
     ↓
┌─────────────────────────────────────┐
│ 【优先级2】查找"可视喂食器"          │
│                                     │
│  找到? ─── YES ──→ 点击进入          │
│    │               ↓                │
│    │           等待3秒               │
│    │               ↓                │
│    │           重新截图OCR           │
│    │               ↓                │
│    │           再找"喂食份数"        │
│    │               ├─ 找到 → ✅ 成功 │
│    │               └─ 没找到 → 重试  │
│   NO                                │
└────┼────────────────────────────────┘
     ↓
┌─────────────────────────────────────┐
│ 【优先级3】查找"首页"                │
│                                     │
│  找到? ─── YES ──→ 点击返回 → 重试   │
│    │                                │
│   NO                                │
└────┼────────────────────────────────┘
     ↓
┌─────────────────────────────────────┐
│ 特殊处理: 检测"涂鸦"(摄像头界面)     │
│                                     │
│  是? ─── YES ──→ 点击左上角返回 → 重试│
│    │                                │
│   NO                                │
└────┼────────────────────────────────┘
     ↓
  等待2秒 → 下一次重试
     ↓
  3次都失败? → ❌ 导航失败
```

---

## 🎲 步骤2: 选择份数并确认

```
前提: 已进入喂食份数弹窗
    ↓
┌─────────────────────────────────────┐
│ 1. 等待1秒 (弹窗完全显示)            │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│ 2. 截图 + OCR识别                    │
│    输出: ['1 份', '2 份', '3 份',   │
│           '确认', '取消']            │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│ 3. 查找份数选项                      │
│                                     │
│  方法1: 完整匹配 "1 份"              │
│    找到? ─── YES ──→ 使用这个位置    │
│      │                              │
│     NO                              │
│      ↓                              │
│  方法2: 只搜索数字 "1"               │
│    在所有文字中找包含"1"的           │
│    例如: "1 份" 包含 "1"            │
│                                     │
│  找到? ─── YES ──→ 点击              │
│      │                              │
│     NO ──→ ❌ 失败                   │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│ 4. 等待0.5秒                         │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│ 5. 重新截图 + 查找"确认"按钮         │
│                                     │
│  找到? ─── YES ──→ 点击 ──→ ✅ 成功  │
│      │                              │
│     NO ──→ ❌ 失败                   │
└─────────────────────────────────────┘
```

---

## 🔍 核心技术: OCR文字匹配

### `_find_text_in_results(all_text, target)`

```python
输入: all_text = [
    ('喂食份数', (194, 703), 0.713),  # (文字, 位置, 置信度)
    ('喂食计划', (65, 702), 0.954),
    ...
]
target = "喂食份数"

处理流程:
    ↓
1. 去除空格
   target_stripped = "喂食份数"
   text_stripped = "喂食份数"
    ↓
2. 模糊匹配 (满足任一即可)
   ✓ target_stripped in text_stripped
   ✓ text_stripped in target_stripped  
   ✓ target in text
   ✓ text in target
    ↓
3. 检查置信度
   confidence >= 0.3  ✓
    ↓
输出: (194, 703)  # 返回位置
```

---

## 📊 状态转换

```
UNKNOWN (未知状态)
    ↓
    ├─ 检测到"首页" ──→ HOME_PAGE
    │
    ├─ 检测到"可视喂食器" ──→ FEEDER_PAGE
    │                          ↓
    │                      点击"喂食份数"
    │                          ↓
    └─ 检测到"喂食份数" ──→ FEEDING_DIALOG
                               ↓
                           选择份数+确认
                               ↓
                           COMPLETED (完成)
```

---

## ⚙️ 关键参数

| 参数 | 值 | 说明 |
|------|-----|------|
| 最大重试次数 | 3 | 导航失败后的重试次数 |
| OCR置信度阈值 | 0.3 | 文字识别的最低置信度 |
| 页面加载等待 | 3秒 | 点击"可视喂食器"后等待 |
| 弹窗显示等待 | 1秒 | 进入喂食弹窗后等待 |
| 点击后延迟 | 0.5-1.0秒 | 点击后等待响应 |

---

## 🎨 优化特性

### ✅ 单次OCR调用
- 每次尝试只调用1次OCR
- 在结果中查找所有目标
- 避免重复计算和结果不一致

### ✅ 优先级明确
```
优先级1: 直接找"喂食份数" (最快)
    ↓ 失败
优先级2: 通过"可视喂食器"导航 (需要1次点击)
    ↓ 失败
优先级3: 返回"首页"重新开始 (需要多次点击)
```

### ✅ 智能匹配
- 去除空格差异
- 支持模糊匹配
- 低置信度阈值

### ✅ 完善调试
- 自动保存每次尝试的截图
- 详细的匹配日志
- 清晰的优先级标记

---

## 📝 典型日志输出

### 成功案例:
```
INFO: 开始自动喂食流程，目标份数: 1
INFO: 导航尝试 1/3
INFO: 当前页面识别到的文字: ['喂食计划', '喂食份数', '喂食记录']
INFO: 【优先级1】查找'喂食份数'按钮...
INFO:   匹配成功: 目标='喂食份数' 实际='喂食份数' 置信度=0.713
INFO: ✓ 找到'喂食份数'按钮，位置: (194, 703)
INFO: 成功进入喂食份数弹窗
INFO: 选择份数: 1
INFO: 找到份数选项，点击
INFO: 找到'确认'按钮，点击
INFO: 自动喂食流程完成 ✅
```

### 需要导航:
```
INFO: 导航尝试 1/3
INFO: 当前页面识别到的文字: ['设备列表', '可视喂食器']
INFO: 【优先级1】查找'喂食份数'按钮...
WARNING: ✗ 未找到'喂食份数'按钮
INFO: 【优先级2】查找'可视喂食器'...
INFO:   匹配成功: 目标='可视喂食器' 实际='可视喂食器' 置信度=0.850
INFO: ✓ 找到'可视喂食器'，位置: (100, 200)，点击进入
INFO: 等待页面加载...
INFO: 进入后识别到的文字: ['喂食计划', '喂食份数', '喂食记录']
INFO:   匹配成功: 目标='喂食份数' 实际='喂食份数' 置信度=0.713
INFO: ✓ 在可视喂食器页面找到'喂食份数'，位置: (194, 703)
INFO: 成功进入喂食份数弹窗 ✅
```

---

## 🎯 使用建议

### 最佳实践:
1. **确保APP在正确页面** - 喂食器控制页面（能看到"喂食份数"按钮）
2. **保持窗口可见** - 不要最小化或遮挡模拟器窗口
3. **检查调试截图** - 失败时查看 `debug_screenshots/` 目录

### 常见问题:
- **识别到但找不到** → 已通过单次OCR调用解决
- **置信度过低** → 已降低阈值到0.3
- **空格导致匹配失败** → 已去除空格后匹配
