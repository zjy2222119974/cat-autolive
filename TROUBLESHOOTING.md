# 冻干喂食器自动化 - 故障排查指南

## 问题诊断

根据日志和截图分析，自动化脚本捕获失败的原因是：

### 当前状态
- ✅ 窗口捕获正常工作
- ✅ OCR识别引擎正常工作
- ❌ APP当前显示的是**摄像头直播界面**，而不是喂食器控制界面

### 截图分析
从 `debug_screenshot.png` 可以看到：
- 顶部显示 "涂鸦" (Tuya智能APP)
- 主界面显示的是宠物摄像头的实时视频画面
- 底部有导航栏

### OCR识别结果
```
识别到的文字: ['涂鸦', '|3361', 'IS=th', "TSs 2'"]
```

这些文字都来自摄像头界面，而自动化脚本期望找到的文字是：
- "喂食份数" - 喂食弹窗
- "可视喂食器" - 设备列表
- "首页" - 主页

## 解决方案

### 方案 1: 手动导航到正确页面（推荐）

在启动自动化之前，请确保APP处于以下任一页面：

1. **设备列表页面** - 显示所有智能设备，包括"可视喂食器"
2. **喂食器控制页面** - 显示喂食器的控制界面

**操作步骤：**
1. 在模拟器中打开涂鸦智能APP
2. 点击返回按钮（左上角 ← 图标）退出摄像头界面
3. 导航到设备列表或喂食器控制页面
4. 然后在主窗口点击"连接"按钮
5. 点击"执行"按钮启动自动喂食

### 方案 2: 改进自动化导航逻辑

我已经在代码中添加了自动检测摄像头界面的逻辑：

```python
# 检测是否在摄像头界面（识别到"涂鸦"等关键词）
if any("涂鸦" in text[0] for text in all_text):
    logger.info("检测到摄像头界面，尝试返回上一页")
    # 尝试点击返回按钮（通常在左上角）
    self.clicker.click_at_position(hwnd, 30, 30, delay=1.0)
    time.sleep(2)
    continue
```

但是，这个方案可能不够稳定，因为：
- 返回按钮的位置可能因APP版本而异
- 可能需要多次点击才能到达正确页面

### 方案 3: 使用调试截图功能

现在，每次OCR识别失败时，系统会自动保存调试截图到：
```
g:/CODE/cat-autolive/debug_screenshots/
```

文件命名格式：
```
nav_fail_YYYYMMDD_HHMMSS_attemptN.png
```

您可以查看这些截图来了解自动化失败时APP的实际状态。

## 测试步骤

### 1. 停止当前运行的程序
在终端中按 `Ctrl+C` 停止正在运行的程序。

### 2. 手动导航到正确页面
在模拟器中：
1. 点击左上角的返回按钮（← 图标）
2. 确保看到设备列表或喂食器控制界面

### 3. 重新启动程序
```bash
python main.py
```

### 4. 连接并执行
1. 在主窗口切换到"控制器"页面
2. 选择"冻干喂食器"
3. 点击"连接"按钮
4. 等待连接成功
5. 点击"执行"按钮

### 5. 查看调试截图
如果仍然失败，检查 `debug_screenshots` 目录中的截图，确认APP的实际状态。

## 技术细节

### 窗口坐标
```
窗口位置: (-443, 592, -225, 1022)
窗口尺寸: 218x430
```
负数坐标表示窗口在副显示器上（位于主显示器左侧）。PIL的 `ImageGrab.grab()` 配合 `all_screens=True` 参数可以正确捕获副显示器上的窗口。

### OCR配置
- 引擎: EasyOCR
- 语言: 中文简体 + 英文
- 运行模式: CPU（如果有GPU会更快）

### 点击模拟
使用Windows API的 `SendMessage` 发送鼠标点击消息到目标窗口。

## 下一步改进建议

1. **添加更多导航状态检测**
   - 识别更多APP界面特征
   - 建立完整的页面状态机

2. **改进点击坐标计算**
   - 使用相对坐标而不是绝对坐标
   - 适配不同分辨率的模拟器

3. **添加UI引导**
   - 在主窗口显示当前检测到的APP状态
   - 提示用户需要手动导航

4. **录制操作流程**
   - 记录成功的导航路径
   - 支持回放功能
